package com.android.memeinn;

/*public class MatchActivityTest extends ActivityInstrumentationTestCase2<MatchStartActivity> {

    @Override
    protected void setUp() throws Exception {
        super.setUp();
        Intent contestIntent = new Intent(getInstrumentation()
                .getTargetContext(), ContestActivity.class);
        contestIntent.putExtra(Global.EXTRA_MESSAGE_OPPONAME, "aaa");
        startActivity(contestIntent, null, null);
        final Button launchNextButton =
                (Button) getActivity()
                        .findViewById(R.id.launch_next_activity_button);
    }
}*/
